apiVersion: v1
kind: Service
metadata:
  name: {{ include "backstage-demo-python-app.fullname" . }}
  labels:
    {{- include "backstage-demo-python-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backstage-demo-python-app.selectorLabels" . | nindent 4 }}
